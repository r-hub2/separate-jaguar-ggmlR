% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nn_layers.R
\name{nn_layer_norm_core}
\alias{nn_layer_norm_core}
\title{Layer-normalize over the feature axis}
\usage{
nn_layer_norm_core(ctx, x, eps)
}
\arguments{
\item{ctx}{A \code{ggml_context}.}

\item{x}{A \code{ggml_tensor} whose features lie along ne[0].}

\item{eps}{Small constant added to the variance.}
}
\value{
A \code{ggml_tensor} of the same shape as \code{x}.
}
\description{
\code{ggml_norm()} normalizes over ne[0] -- the feature axis for every input
rank the layers use -- keeping the sequence and batch axes, so one call
covers flat and sequence inputs alike. Upstream ggml has no backward rule for
it; ggmlR adds \code{GGML_OP_NORM_BACK} (CPU and Vulkan kernels), so a graph
using this is trainable.
}
\keyword{internal}
