% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operations.R
\name{ggml_ssm_scan_output}
\alias{ggml_ssm_scan_output}
\alias{ggml_ssm_scan_state}
\title{Views Into a Packed ssm_scan Result}
\usage{
ggml_ssm_scan_output(ctx, result, x)

ggml_ssm_scan_state(ctx, result, s, ids)
}
\arguments{
\item{ctx}{GGML context}

\item{result}{The tensor returned by \code{\link{ggml_ssm_scan}}}

\item{x}{The \code{x} passed to the scan (gives the output shape)}

\item{s}{The \code{s} passed to the scan (gives the state shape)}

\item{ids}{The \code{ids} passed to the scan (gives the sequence count)}
}
\value{
For \code{ggml_ssm_scan_output()}, a tensor shaped like \code{x}

For \code{ggml_ssm_scan_state()}, a tensor
  \code{[d_state, head_dim, n_head, n_seqs]}
}
\description{
\code{\link{ggml_ssm_scan}} returns the outputs and the final states in one
flat tensor.  These helpers view either half with the right shape, so the
offset arithmetic lives in one place: \code{ggml_ssm_scan_output()} yields
the outputs shaped like \code{x}, and \code{ggml_ssm_scan_state()} the final
states shaped like the leading \code{n_seqs} rows of \code{s}.
}
\details{
Both are graph views sharing memory with \code{result}; they add no
computation.  \code{x}, \code{s} and \code{ids} must be the same tensors the
scan was built with, since their shapes are what the offsets are derived
from.
}
\seealso{
Other state-space: 
\code{\link{ggml_gated_linear_attn}()},
\code{\link{ggml_gated_linear_attn_back}()},
\code{\link{ggml_rwkv_output}()},
\code{\link{ggml_rwkv_wkv6}()},
\code{\link{ggml_rwkv_wkv6_back}()},
\code{\link{ggml_rwkv_wkv7}()},
\code{\link{ggml_rwkv_wkv7_back}()},
\code{\link{ggml_ssm_conv}()},
\code{\link{ggml_ssm_conv_back}()},
\code{\link{ggml_ssm_scan}()}
}
\concept{state-space}
