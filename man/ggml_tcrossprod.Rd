% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpu_linalg.R
\name{ggml_tcrossprod}
\alias{ggml_tcrossprod}
\title{GPU transposed cross product (drop-in for \code{tcrossprod})}
\usage{
ggml_tcrossprod(A, B = NULL, device = "auto", prec = "f32")
}
\arguments{
\item{A}{A numeric matrix.}

\item{B}{A numeric matrix with \code{ncol(B) == ncol(A)}, or \code{NULL}
(default) for \code{A \%*\% t(A)}.}

\item{device, prec}{See \code{\link{ggml_matmul}}.}
}
\value{
\code{A \%*\% t(B)} as a numeric \code{matrix}.
}
\description{
Computes \code{A \%*\% t(B)} (or \code{A \%*\% t(A)} when \code{B} is
\code{NULL}) on the Vulkan GPU, returning an R matrix, with a CPU fallback.
See \code{\link{ggml_matmul}} for the precision and dispatch notes.
}
