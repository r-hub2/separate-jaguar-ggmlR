% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ag_training.R
\name{lr_scheduler_warmup_cosine}
\alias{lr_scheduler_warmup_cosine}
\title{Linear warmup followed by cosine annealing}
\usage{
lr_scheduler_warmup_cosine(
  optimizer,
  warmup_steps,
  total_steps,
  lr_min = 0,
  warmup_start_lr = 0
)
}
\arguments{
\item{optimizer}{Optimizer environment.  Its current \code{lr} is taken as
the peak learning rate.}

\item{warmup_steps}{Number of linear warmup steps.}

\item{total_steps}{Total number of \code{$step()} calls (warmup included).}

\item{lr_min}{Final learning rate after annealing (default 0).}

\item{warmup_start_lr}{Learning rate at step 0 (default 0).}
}
\value{
An \code{lr_scheduler_warmup_cosine} environment with \code{$step()}
  and \code{$get_lr()}.
}
\description{
Raises the learning rate linearly from \code{warmup_start_lr} to the
optimizer's initial \code{lr} over \code{warmup_steps} steps, then anneals it
to \code{lr_min} following a cosine curve over the remaining
\code{total_steps - warmup_steps} steps.  This is the standard transformer
training schedule.  Call \code{$step()} \strong{per batch}.
}
\examples{
\donttest{
w   <- ag_param(matrix(runif(4), 2, 2))
opt <- optimizer_adam(list(w = w), lr = 1e-3)
sch <- lr_scheduler_warmup_cosine(opt, warmup_steps = 10L, total_steps = 100L)
for (i in 1:100) sch$step()
}
}
