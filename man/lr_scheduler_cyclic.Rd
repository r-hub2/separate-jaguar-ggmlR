% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ag_training.R
\name{lr_scheduler_cyclic}
\alias{lr_scheduler_cyclic}
\title{Cyclical learning rate scheduler}
\usage{
lr_scheduler_cyclic(
  optimizer,
  base_lr,
  max_lr,
  step_size_up = 2000L,
  step_size_down = NULL,
  mode = c("triangular", "triangular2", "exp_range"),
  gamma = 1
)
}
\arguments{
\item{optimizer}{Optimizer environment.}

\item{base_lr}{Lower learning rate bound.}

\item{max_lr}{Upper learning rate bound.}

\item{step_size_up}{Steps in the increasing half of a cycle (default 2000).}

\item{step_size_down}{Steps in the decreasing half.  \code{NULL} (default)
mirrors \code{step_size_up}.}

\item{mode}{One of \code{"triangular"} (default), \code{"triangular2"}
(amplitude halved each cycle) or \code{"exp_range"} (amplitude scaled by
\code{gamma^step}).}

\item{gamma}{Decay factor for \code{mode = "exp_range"} (default 1.0).}
}
\value{
An \code{lr_scheduler_cyclic} environment with \code{$step()} and
  \code{$get_lr()}.
}
\description{
Cycles the learning rate linearly between \code{base_lr} and \code{max_lr}
over a triangular cycle of \code{2 * step_size_up} steps (Smith, 2017).
Each call to \code{$step()} advances one step — call it \strong{per batch},
not per epoch.
}
\examples{
\donttest{
w   <- ag_param(matrix(runif(4), 2, 2))
opt <- optimizer_sgd(list(w = w), lr = 0.01)
sch <- lr_scheduler_cyclic(opt, base_lr = 0.001, max_lr = 0.01,
                           step_size_up = 10L)
for (i in 1:40) sch$step()
}
}
\references{
Smith, L. N. (2017). Cyclical Learning Rates for Training Neural
  Networks. WACV.
}
