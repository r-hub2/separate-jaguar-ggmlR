% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ag_training.R
\name{lr_scheduler_onecycle}
\alias{lr_scheduler_onecycle}
\title{One-cycle learning rate scheduler}
\usage{
lr_scheduler_onecycle(
  optimizer,
  max_lr,
  total_steps,
  pct_start = 0.3,
  div_factor = 25,
  final_div_factor = 10000,
  anneal_strategy = c("cos", "linear"),
  cycle_momentum = TRUE,
  base_momentum = 0.85,
  max_momentum = 0.95
)
}
\arguments{
\item{optimizer}{Optimizer environment.}

\item{max_lr}{Peak learning rate.}

\item{total_steps}{Total number of \code{$step()} calls for the whole run.}

\item{pct_start}{Fraction of \code{total_steps} spent increasing the LR
(default 0.3).}

\item{div_factor}{Initial LR is \code{max_lr / div_factor} (default 25).}

\item{final_div_factor}{Final LR is \code{max_lr / final_div_factor}
(default 1e4).}

\item{anneal_strategy}{\code{"cos"} (default) or \code{"linear"}.}

\item{cycle_momentum}{Logical; cycle momentum inversely to LR (default
\code{TRUE}).}

\item{base_momentum, max_momentum}{Momentum bounds used when
\code{cycle_momentum = TRUE} (defaults 0.85 / 0.95).}
}
\value{
An \code{lr_scheduler_onecycle} environment with \code{$step()} and
  \code{$get_lr()}.
}
\description{
Implements the 1cycle policy (Smith, 2018): the learning rate rises from
\code{max_lr / div_factor} to \code{max_lr} during the first
\code{pct_start} fraction of training, then anneals down to
\code{max_lr / final_div_factor}.  Call \code{$step()} \strong{per batch};
\code{total_steps} is the total number of batches over the whole run
(\code{epochs * batches_per_epoch}).
}
\details{
When \code{cycle_momentum = TRUE} the momentum-like hyper-parameter moves
inversely to the learning rate: \code{$momentum} for SGD, \code{$beta1} for
Adam.  For other optimizers momentum cycling is skipped.
}
\examples{
\donttest{
w   <- ag_param(matrix(runif(4), 2, 2))
opt <- optimizer_sgd(list(w = w), lr = 0.01, momentum = 0.9)
sch <- lr_scheduler_onecycle(opt, max_lr = 0.1, total_steps = 100L)
for (i in 1:100) sch$step()
}
}
\references{
Smith, L. N. (2018). A disciplined approach to neural network
  hyper-parameters. arXiv:1803.09820.
}
