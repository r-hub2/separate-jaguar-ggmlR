% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpu_linalg.R
\name{ggml_crossprod}
\alias{ggml_crossprod}
\title{GPU cross product (drop-in for \code{crossprod})}
\usage{
ggml_crossprod(A, B = NULL, device = "auto", prec = "f32")
}
\arguments{
\item{A}{A numeric matrix.}

\item{B}{A numeric matrix with \code{nrow(B) == nrow(A)}, or \code{NULL}
(default) for \code{t(A) \%*\% A}.}

\item{device, prec}{See \code{\link{ggml_matmul}}.}
}
\value{
\code{t(A) \%*\% B} as a numeric \code{matrix}.
}
\description{
Computes \code{t(A) \%*\% B} (or \code{t(A) \%*\% A} when \code{B} is
\code{NULL}) on the Vulkan GPU, returning an R matrix, with a CPU fallback.
See \code{\link{ggml_matmul}} for the precision and dispatch notes.
}
