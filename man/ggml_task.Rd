% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sc_contracts.R
\name{ggml_task}
\alias{ggml_task}
\title{Construct a single-cell compute task}
\usage{
ggml_task(op, matrix, params = list(), device = c("auto", "vulkan", "cpu"))
}
\arguments{
\item{op}{Operation name; must be registered in
\code{\link{ggml_ops_registry}} (e.g. \code{"embed"}).}

\item{matrix}{A numeric \code{matrix} (dense) or \code{dgCMatrix} (sparse).
Rows are features (genes), columns are cells.}

\item{params}{Named list of operation parameters (e.g. \code{n_components}).}

\item{device}{\code{"vulkan"}, \code{"cpu"} or \code{"auto"} (default).}
}
\value{
An object of class \code{ggml_task}.
}
\description{
A \code{ggml_task} is the contract object passed from the extraction layer to
the dispatch layer. It bundles the operation name, the dense feature matrix
(features in rows, cells in columns — the single-cell convention), the
operation parameters and the requested device. It performs no computation.
}
\seealso{
\code{\link{ggml_run}}, \code{\link{ggml_ops_registry}}
}
