% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/piece_transformer.R
\name{pt_transformer_load}
\alias{pt_transformer_load}
\title{Build a piece transformer and load a checkpoint into it}
\usage{
pt_transformer_load(path, layout = pt_cube4_layout(), ...)
}
\arguments{
\item{path}{Path to the \code{.pth} checkpoint.}

\item{layout}{A layout list; defaults to \code{\link{pt_cube4_layout}}.}

\item{...}{Passed to \code{\link{pt_transformer}} --- \code{d_model},
\code{n_heads} and the rest, when they differ from the defaults.}
}
\value{
A \code{pt_transformer} with the checkpoint's weights.
}
\description{
Reads a \code{.pth} saved by \code{torch.save()} and returns the model ready
to run. Everything that differs between torch's conventions and this
package's is resolved here rather than at inference time.
}
\examples{
\dontrun{
model <- pt_transformer_load("model.pth")
q <- pt_forward(model, rep(0:5, each = 16))
which.max(q)
}
}
\seealso{
\code{\link{pth_catalogue}} to see what a checkpoint holds.
}
