% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gpu_linalg.R
\name{as_gpu_matrix}
\alias{as_gpu_matrix}
\title{Wrap a matrix so multiplies run on the GPU}
\usage{
as_gpu_matrix(x, device = "auto", prec = "f32")
}
\arguments{
\item{x}{A numeric matrix (or something coercible with \code{as.matrix}).}

\item{device}{\code{"auto"} (default), \code{"gpu"} or \code{"cpu"}; see
\code{\link{ggml_matmul}}.}

\item{prec}{\code{"f32"} (default) or \code{"f16"}; see \code{\link{ggml_matmul}}.}
}
\value{
A \code{\linkS4class{ggml_matrix}}.
}
\description{
Tags a matrix as GPU-backed. In an expression like
\code{as_gpu_matrix(A) \%*\% B}, the multiply dispatches to
\code{\link{ggml_matmul}} (Vulkan GPU with CPU fallback) instead of R's
built-in. Only one operand needs wrapping. \code{crossprod()} and
\code{tcrossprod()} on a wrapped matrix likewise use the GPU.
}
\examples{
A <- matrix(rnorm(9), 3)
B <- matrix(rnorm(9), 3)
as_gpu_matrix(A, device = "cpu") \%*\% B
}
