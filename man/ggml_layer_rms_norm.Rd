% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/nn_layers.R
\name{ggml_layer_rms_norm}
\alias{ggml_layer_rms_norm}
\title{Add RMS Normalization Layer}
\usage{
ggml_layer_rms_norm(model, eps = 1e-05, name = NULL, trainable = TRUE)
}
\arguments{
\item{model}{A \code{ggml_sequential_model} or a \code{ggml_tensor_node}
(functional API).}

\item{eps}{Small constant for numerical stability (default 1e-5)}

\item{name}{Optional character name for the layer.}

\item{trainable}{Logical; whether the layer weights are updated during training.}
}
\value{
The model with the layer appended, or a new \code{ggml_tensor_node}
  in the functional API.
}
\description{
Normalizes each sample by the root mean square of its features, then scales
by gamma and shifts by beta (both learnable). Unlike
\code{\link{ggml_layer_batch_norm}} it does not subtract the mean and keeps
no running statistics, so it behaves identically in training and inference
and does not couple the samples in a batch. This is the normalization used
by transformer blocks.
}
\examples{
\donttest{
# Functional API: the normalization of a transformer block.
x <- ggml_input(shape = c(10L, 32L))
h <- x |> ggml_layer_rms_norm()
h <- h |> ggml_layer_attention(d_model = 32L, n_heads = 4L)
}
}
\seealso{
\code{\link{ggml_layer_batch_norm}}
}
