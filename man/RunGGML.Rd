% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sc_sce.R, R/sc_seurat.R
\name{RunGGML.SingleCellExperiment}
\alias{RunGGML.SingleCellExperiment}
\alias{RunGGML}
\alias{RunGGML.default}
\alias{RunGGML.Seurat}
\title{Run a GGML GPU operation on a Seurat object}
\usage{
\method{RunGGML}{SingleCellExperiment}(
  object,
  op = "embed",
  assay = NULL,
  layer = NULL,
  n_components = 50L,
  reduction_name = "ggml",
  device = "auto",
  genes = NULL,
  cells = NULL,
  reduction = NULL,
  dims = NULL,
  ...
)

RunGGML(
  object,
  op = "embed",
  assay = NULL,
  layer = NULL,
  n_components = 50L,
  reduction_name = "ggml",
  device = "auto",
  genes = NULL,
  cells = NULL,
  reduction = NULL,
  dims = NULL,
  ...
)

\method{RunGGML}{default}(
  object,
  op = "embed",
  assay = NULL,
  layer = NULL,
  n_components = 50L,
  reduction_name = "ggml",
  device = "auto",
  genes = NULL,
  cells = NULL,
  ...
)

\method{RunGGML}{Seurat}(
  object,
  op = "embed",
  assay = NULL,
  layer = NULL,
  n_components = 50L,
  reduction_name = "ggml",
  device = "auto",
  genes = NULL,
  cells = NULL,
  reduction = NULL,
  dims = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{Seurat} or \code{SingleCellExperiment} object, or a bare
feature-by-cell \code{matrix}/\code{dgCMatrix} (the \code{.default} method
returns a \code{\link{ggml_result}} instead of an object).}

\item{op}{Operation name; see \code{\link{ggml_ops_registry}}. Default
\code{"embed"}.}

\item{assay}{Assay to read (Seurat); defaults to the object's default assay.}

\item{layer}{Layer/slot to read; default \code{"data"}.}

\item{n_components}{Number of components for \code{"embed"} (PCA). Default 50.}

\item{reduction_name}{Name of the reduction slot to create. Default
\code{"ggml"}.}

\item{device}{\code{"auto"} (default), \code{"vulkan"} or \code{"cpu"}.}

\item{genes, cells}{Optional feature/cell subsets passed to extraction.}

\item{reduction}{Optional name of an existing reduction to use as the input
feature space (e.g. \code{"pca"} for \code{"umap"} / \code{"neighbors"}),
instead of an assay layer. Seurat: \code{Embeddings()}; SCE:
\code{reducedDim()}.}

\item{dims}{Optional integer vector selecting columns of \code{reduction}.}

\item{...}{Additional parameters forwarded to the engine.}
}
\value{
For a Seurat object, the updated object with a new reduction. For a
  bare matrix, a \code{\link{ggml_result}}.
}
\description{
High-level, Seurat-style entry point: extracts the expression matrix from the
object, runs the requested operation on the GGML backend (Vulkan GPU with CPU
fallback) and writes the result back into the object. Returns the updated
object, so it slots into a \code{\%>\%} / \code{|>} pipeline next to
\code{Seurat::RunPCA()}. Methods exist for \code{Seurat} and
\code{SingleCellExperiment} objects (and a \code{.default} for bare matrices).
}
\details{
Supported operations include \code{"embed"} (PCA), \code{"normalize"},
\code{"scale"}, \code{"umap"} and \code{"neighbors"}; see
\code{\link{ggml_ops_registry}}.
}
\examples{
\dontrun{
library(Seurat)
pbmc <- RunGGML(pbmc, op = "embed", n_components = 30)
DimPlot(pbmc, reduction = "ggml")
}
}
