% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operations.R
\name{ggml_rwkv_output}
\alias{ggml_rwkv_output}
\alias{ggml_rwkv_state}
\title{Views Into a Packed RWKV / GLA Result}
\usage{
ggml_rwkv_output(ctx, result, k)

ggml_rwkv_state(ctx, result, k, state)
}
\arguments{
\item{ctx}{GGML context}

\item{result}{The tensor returned by one of the RWKV / GLA ops}

\item{k}{The \code{k} passed to that op (gives \code{S}, \code{H} and the
token count)}

\item{state}{The \code{state} passed to that op (gives the sequence count)}
}
\value{
For \code{ggml_rwkv_output()}, a tensor \code{[S * H, n_tokens]}

For \code{ggml_rwkv_state()}, a tensor \code{[S * H, S * n_seqs]}
}
\description{
\code{\link{ggml_rwkv_wkv6}}, \code{\link{ggml_rwkv_wkv7}} and
\code{\link{ggml_gated_linear_attn}} all return one
\code{[S * H, n_tokens + S * n_seqs]} tensor holding the per-token outputs
followed by the final state.  These helpers view either half, so the column
arithmetic lives in one place rather than at every call site.
}
\details{
Both are graph views sharing memory with \code{result} and add no
computation.  \code{k} must be the same tensor the op was built with, since
\code{S}, \code{H} and \code{n_tokens} are read from it.
}
\seealso{
Other state-space: 
\code{\link{ggml_gated_linear_attn}()},
\code{\link{ggml_gated_linear_attn_back}()},
\code{\link{ggml_rwkv_wkv6}()},
\code{\link{ggml_rwkv_wkv6_back}()},
\code{\link{ggml_rwkv_wkv7}()},
\code{\link{ggml_rwkv_wkv7_back}()},
\code{\link{ggml_ssm_conv}()},
\code{\link{ggml_ssm_conv_back}()},
\code{\link{ggml_ssm_scan}()},
\code{\link{ggml_ssm_scan_output}()}
}
\concept{state-space}
