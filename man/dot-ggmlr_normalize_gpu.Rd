% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sc_contracts.R
\name{.ggmlr_normalize_gpu}
\alias{.ggmlr_normalize_gpu}
\title{GPU-accelerated LogNormalize (op = "normalize")}
\usage{
.ggmlr_normalize_gpu(mat, scale_factor = 10000, backend = c("vulkan", "cpu"))
}
\arguments{
\item{mat}{Dense numeric matrix, features x cells (raw/counts).}

\item{scale_factor}{Library size to scale each cell to (default 1e4).}

\item{backend}{\code{"vulkan"} or \code{"cpu"} (dispatch resolves "auto").}
}
\value{
A \code{\link{ggml_result}} whose \code{embedding} is the normalised
  features x cells matrix; \code{metadata$kind = "transform"},
  \code{metadata$layer = "data"}.
}
\description{
Library-size normalisation followed by log1p, matching Seurat's
\code{NormalizeData(method = "LogNormalize")}:
\code{log1p(x / colSums(x) * scale_factor)}. The per-cell scaling and the
\code{log1p} run elementwise on the GPU (broadcast a per-cell factor across
genes); the column sums are a cheap reduction.
}
\keyword{internal}
