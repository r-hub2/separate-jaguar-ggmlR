% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/operations.R
\name{ggml_gated_linear_attn}
\alias{ggml_gated_linear_attn}
\title{Gated Linear Attention}
\usage{
ggml_gated_linear_attn(ctx, k, v, q, g, state, scale)
}
\arguments{
\item{ctx}{GGML context}

\item{k}{Keys, \code{[S, H, n_tokens]}}

\item{v}{Values, \code{[S, H, n_tokens]}}

\item{q}{Queries, \code{[S, H, n_tokens]}}

\item{g}{Gate, \code{[S, H, n_tokens]}}

\item{state}{Initial state, \code{S * S * H * n_seqs} elements}

\item{scale}{Scalar applied to the queries (usually \code{1 / sqrt(S)})}
}
\value{
A tensor \code{[S * H, n_tokens + S * n_seqs]}
}
\description{
Linear attention with a per-token gate (the GLA / RetNet family).  Shares the
RWKV recurrence machinery: keys, values and queries drive a running state
that \code{g} gates, and \code{scale} multiplies the query.
}
\section{Shapes}{

\code{k}, \code{v}, \code{q} and \code{g} are \code{[S, H, n_tokens]};
\code{state} holds \code{S * S * H * n_seqs} elements.  The result is
\code{[S * H, n_tokens + S * n_seqs]}, outputs then final state -- see
\code{\link{ggml_rwkv_output}}.

\code{n_tokens} counts the tokens of \emph{all} sequences together, so it
must be a multiple of \code{n_seqs}.
}

\section{Backend support}{

CPU only.  Unlike the RWKV and SSM ops, \code{GGML_OP_GATED_LINEAR_ATTN} has
no Vulkan shader, so a graph containing it falls back to the CPU for this
node.  It has no backward pass either, so it is inference-only.
}

\seealso{
\code{\link{ggml_rwkv_wkv7}}, \code{\link{ggml_rwkv_output}}

Other state-space: 
\code{\link{ggml_gated_linear_attn_back}()},
\code{\link{ggml_rwkv_output}()},
\code{\link{ggml_rwkv_wkv6}()},
\code{\link{ggml_rwkv_wkv6_back}()},
\code{\link{ggml_rwkv_wkv7}()},
\code{\link{ggml_rwkv_wkv7_back}()},
\code{\link{ggml_ssm_conv}()},
\code{\link{ggml_ssm_conv_back}()},
\code{\link{ggml_ssm_scan}()},
\code{\link{ggml_ssm_scan_back}()},
\code{\link{ggml_ssm_scan_output}()}
}
\concept{state-space}
