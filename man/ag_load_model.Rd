% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ag_save.R
\name{ag_load_model}
\alias{ag_load_model}
\title{Load an autograd module from a saved state}
\usage{
ag_load_model(path, model_fn = NULL, device = NULL)
}
\arguments{
\item{path}{File path written by \code{\link{ag_save_model}}.}

\item{model_fn}{Optional zero-argument rebuild function. Required if no
\code{model_fn} was stored at save time. If both are present, this argument
takes precedence.}

\item{device}{Optional device for the rebuilt module (\code{"cpu"} or
\code{"gpu"}). If \code{NULL} (default), the current \code{ag_device()} is
used by the rebuild.}
}
\value{
The reconstructed module with restored weights, in eval mode.
}
\description{
Reconstructs an \code{ag_*} module saved with \code{\link{ag_save_model}}.
The architecture is rebuilt by calling \code{model_fn} (either the one passed
here, or the one stored inside the container at save time), and the saved
parameter and buffer values are copied back by name.
}
\examples{
\donttest{
build <- function() ag_sequential(ag_linear(4L, 8L), ag_linear(8L, 3L))
f <- tempfile(fileext = ".rds")
ag_save_model(build(), f, model_fn = build)
model <- ag_load_model(f)
}
}
\seealso{
\code{\link{ag_save_model}}
}
